import { Languages } from "../../../types";
interface CreateErrorScreenElementParams {
    id: string;
    onRetry: VoidFunction;
    onClose: (isFromButton?: boolean) => void;
    lang: Languages;
}
export declare function createErrorScreenElement({ id, onClose, onRetry, lang }: CreateErrorScreenElementParams): {
    modalDiv: HTMLDivElement;
};
export {};
