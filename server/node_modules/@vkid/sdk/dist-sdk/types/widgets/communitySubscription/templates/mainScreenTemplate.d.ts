import { Languages } from "../../../types";
interface CreateMainScreenElementParams {
    id: string;
    onSubmit: VoidFunction;
    onClose: (isFromButton?: boolean) => void;
    userAvatarUrls?: string[];
    groupAvatarUrl: string;
    membersCount: number;
    friendsCount?: number;
    description: string;
    title: string;
    isVerified?: boolean;
    lang: Languages;
}
export declare function createMainScreenElement({ id, onClose, onSubmit, userAvatarUrls, friendsCount, membersCount, title, description, groupAvatarUrl, isVerified, lang, }: CreateMainScreenElementParams): {
    modalDiv: HTMLDivElement;
    setLoading: () => void;
};
export {};
