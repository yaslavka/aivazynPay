import type { Config } from "../../core/config";
import { CommunitySubscriptionGroupParams } from "../../widgets/communitySubscription/types";
import { AccountGetProfileShortInfoResponse, GetGroupInfoResponse, GroupsJoinResponse } from './types';
export declare class CommunitySubscriptionService {
    private readonly config;
    private accessToken;
    private groupId;
    constructor(config: Config);
    private sendError;
    init({ accessToken, groupId }: {
        accessToken: string;
        groupId: number;
    }): Promise<CommunitySubscriptionGroupParams>;
    getProfileShortInfo(): Promise<AccountGetProfileShortInfoResponse>;
    getGroupInfo(): Promise<GetGroupInfoResponse>;
    joinGroup(): Promise<GroupsJoinResponse>;
}
